_format_version: "3.0"

patches:
  - selectors:
    - $.services[?(@.name=="bookings-service-fca")]
    values:
      host: bookings.kongair
      port: 8082
      protocol: http
      plugins:
        - name: jwt
          _config: default-jwt
          enabled: true
